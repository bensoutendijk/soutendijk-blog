<!DOCTYPE html>
<html>
  <head>
    <%= favicon_link_tag 'favicon.ico' %>
    <title>Boozy Heights</title>
    <%= csrf_meta_tags %>
    
    <!--Google Fonts:-->
    
      <!--Barrio-->
    <link href="https://fonts.googleapis.com/css?family=Barrio" rel="stylesheet">
    
      <!--Josefin Sans-->
    <link href="https://fonts.googleapis.com/css?family=Josefin+Sans:400,600,700" rel="stylesheet">
    
      <!--Raleway-->
    <link href="https://fonts.googleapis.com/css?family=Raleway:400,700,800" rel="stylesheet">
    
  
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <nav class="navbar navbar-static">
    <h1 class="text-center josefin">Boozy Heights</h1>
    <%= nav_link "Read", root_path, 'left' %>
    <%= nav_link "Sign in", new_user_session_path, 'right' %>
    <%= nav_link "New Posts", new_article_path, 'right' %>
    <%= nav_link "About", about_path, 'right' %>
  </nav>
  <div class="container">
    <div class="row">
      <h3 class="text-center blurb">"What else can change your ideas...like whisky?"</h3>
    </div>
  </div>
  <div class="container flash-container">
  <% flash.each do |key, value| %>
    <% if value.class == Array %>
      <div class="<%= flash_class(key) %>">
        <button type="button" class="close" data-dismiss="alert">×</button>
        <% value.each do |message| %>  
          <%= message %>
          </br>
        <% end %>
      </div>
    <% else %>
      <div class="<%= flash_class(key) %>">
        <button type="button" class="close" data-dismiss="alert">×</button>
        <%= value %>
      </div>
    <% end %>
  <% end %>
  </div>
  
  <div class="container-fluid turbo-body">
    <%= yield %>
  </div>
</html>
